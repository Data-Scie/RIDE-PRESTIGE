DATABASE_URL="postgresql://rp_user:rp_secret_2026@localhost:5432/ride_prestige?schema=public"
DIRECT_URL="postgresql://rp_user:rp_secret_2026@localhost:5432/ride_prestige?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="7d"
PORT=4000
NODE_ENV="development"
GOOGLE_MAPS_DISTANCE_MATRIX_API_KEY=""
RESEND_API_KEY=""
MAIL_FROM="Ride Prestige <bookings@rideprestige.co.uk>"
OPERATIONS_EMAIL="bookings@rideprestige.co.uk"
# Sentry error tracking DSN. Leave empty to disable entirely - see HANDOVER.md.
SENTRY_DSN=""
# The web app's public URL, for CORS and Stripe Checkout redirect URLs.
WEB_ORIGIN="https://ride-prestige-sigma.vercel.app"
# Stripe secret key + webhook signing secret. Leave both empty to disable payments entirely -
# the booking flow works exactly as before (no payment step). See HANDOVER.md.
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
# Twilio SMS. Leave empty to disable SMS entirely - see HANDOVER.md.
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM_NUMBER=""
